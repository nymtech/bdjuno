table:
  schema: public
  name: wasm_params
select_permissions:
  - role: anonymous
    permission:
      columns:
        - one_row_id
        - code_upload_access
        - instantiate_default_permission
        - max_wasm_code_size
        - height
      filter: {}
      allow_aggregations: true
