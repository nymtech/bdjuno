table:
  name: wasm_params
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - one_row_id
        - code_upload_access
        - instantiate_default_permission
        - height
      filter: {}
      allow_aggregations: true
